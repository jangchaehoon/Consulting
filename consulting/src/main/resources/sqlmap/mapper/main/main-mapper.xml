<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Main">

	<select id="dbTest" parameterType="com.yp.main.MainVO" resultType="hmap">
		/* main-mapper.xml id="dbTest" 데이터베이스 연결 테스트 */
		SELECT tb1.id
			 , tb1.pwd
		  FROM test tb1
	</select>
	
</mapper>